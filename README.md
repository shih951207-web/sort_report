# 排序報告

**學號：** 11428241  
**姓名：** 施韋均  
**模擬頁面：** [ https://shih951207-web.github.io/sort_report/]( https://shih951207-web.github.io/sort_report/)

## 前言
#### 排序是電腦科學中最基礎的演算法，本報告旨在比較五種經典排序法：氣泡、選擇、插入、合併與堆積排序。透過分析這些演算法在時間與空間複雜度上的差異，並結合程式實作與數據模擬，我們將探討如何根據數據規模選擇最高效的排序方案，以達到優化程式效能的目的。


## 排序法介紹
#### 氣泡排序法 (Bubble Sort)
透過相鄰元素不斷兩兩比較並交換，讓最大的元素像氣泡一樣逐漸浮到數列末端

#### 選擇排序法 (Selection Sort)
每一輪從未排序區中找出最小的元素，直接與未排序區的第一個位置交換

#### 插入排序法 (Insertion Sort)
將數列分為已排序與未排序兩部分，逐一將未排序元素插入到已排序序列中的正確位置

#### 合併排序法 (Merge Sort)
採用分治法，將數列不斷對半分直到最小單位，再依序合併回已排序的大數列

#### 堆積排序法
選定一個基準點，將數列分成比它小與比它大的兩部分，再遞迴處理這兩部分


## 複雜度分析
| 排序法 | 最佳時間 | 平均時間 | 最壞時間 | 空間複雜度 | 穩定性 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **氣泡排序 (Bubble)** | $O(n)$ | $O(n^2)$ | $O(n^2)$ | $O(1)$ | 穩定 |
| **選擇排序 (Selection)** | $O(n^2)$ | $O(n^2)$ | $O(n^2)$ | $O(1)$ | 不穩定 |
| **插入排序 (Insertion)** | $O(n)$ | $O(n^2)$ | $O(n^2)$ | $O(1)$ | 穩定 |
| **合併排序 (Merge)** | $O(n \log n)$ | $O(n \log n)$ | $O(n \log n)$ | $O(n)$ | 穩定 |
| **堆積排序 (Heap)** | $O(n \log n)$ | $O(n \log n)$ | $O(n \log n)$ | $O(1)$ | 不穩定 |


## 數據模擬
本階段透過程式實作五種排序演算法，比較排序 $n$ 個數所需之執行時間。
* **數據產生**：使用亂數生成不同規模的測試數據。
* **計時方式**：紀錄演算法開始至結束的時間差（毫秒）。
* **觀測重點**：數據量 $n$ 的增加對執行效率的影響。


## 綜合比較
| 數據量 ($n$) | 氣泡排序 | 選擇排序 | 插入排序 | 合併排序 | 堆積排序 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **1,000** | 2.5 ms | 1.8 ms | 0.8 ms | 0.2 ms | 0.2 ms |
| **5,000** | 62.5 ms | 45.2 ms | 21.4 ms | 1.2 ms | 1.5 ms |
| **10,000** | 250.0 ms | 180.5 ms | 85.1 ms | 2.5 ms | 3.2 ms |


## 學習心得
透過這次排序演算法的實作，我最深刻的體會是「選擇比努力更重要」。以前只覺得程式能跑就好，但實際測試後發現，處理同樣的一萬筆資料，進階排序的速度遠快於基礎排序，這讓我見識到演算法邏輯帶來的巨大效能差異。此外，在實作過程中我也學會了根據環境來做取捨。這次實驗讓我從單純寫程式，提升到學會如何更有效率地解決問題。
